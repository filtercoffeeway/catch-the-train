TELEGRAM_TOKEN=123456:ABC-your-bot-token
# BART_KEY=your-own-bart-key   (defaults to BART's public key)

# SQLite file holding every user's settings and alert state.
DB_PATH=catchthetrain.db
# New sign-ups are refused once this many users have set up.
MAX_USERS=50
